#include <stdio.h>

int main(){
	char s[1000000];
	gets(s);
	for(int i=0;i<strlen(s);i++){
		if(s[i]==' ') continue;
		else if((s[i]>'z'||s[i]<'a')&&(s[i]>'Z'&&s[i]<'A')) printf("\n");
		else printf("%d",s[i]);
	}
	return 0;
}
